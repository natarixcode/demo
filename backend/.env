# =================================
# NOTORIX BACKEND ENVIRONMENT CONFIG
# =================================
# RENAME THIS FILE TO: .env

# Server Configuration
PORT=3001
NODE_ENV=development

# =================================
# DATABASE CONFIGURATION (PostgreSQL)
# =================================

# OPTION 1: Connection String Format (Recommended)
# Replace 'your_password_here' with your actual PostgreSQL password
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/notorix

# OPTION 2: Individual Database Parameters (Alternative)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=notorix
DB_USER=postgres
DB_PASSWORD=your_password_here

# =================================
# COMMON PASSWORD OPTIONS TO TRY:
# =================================
# If you're not sure about your password, try these common ones:
# DATABASE_URL=postgresql://postgres@localhost:5432/notorix
# DATABASE_URL=postgresql://postgres:admin@localhost:5432/notorix
# DATABASE_URL=postgresql://postgres:password@localhost:5432/notorix
# DATABASE_URL=postgresql://postgres:123456@localhost:5432/notorix
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notorix

# =================================
# AUTHENTICATION & SECURITY
# =================================

# JWT Secret for authentication (change this in production)
JWT_SECRET=notorix_super_secret_key_2024_development_only

# Session Secret
SESSION_SECRET=notorix_session_secret_key_development

# =================================
# API CONFIGURATION
# =================================

# API Base URL
API_BASE_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:4173

# =================================
# LOGGING & DEBUG
# =================================

# Log Level (error, warn, info, debug)
LOG_LEVEL=debug

# Enable SQL Query Logging
DB_LOGGING=true

# =================================
# BACKUP CONFIGURATION (MongoDB - if needed)
# =================================

# Keep MongoDB URI as backup option
MONGODB_URI=mongodb://localhost:27017/notorix

# =================================
# INSTRUCTIONS FOR SETUP:
# =================================

# 1. FIND YOUR POSTGRESQL PASSWORD:
#    - Check pgAdmin (if you use it)
#    - Try common passwords: admin, password, postgres, 123456
#    - Or reset it: ALTER USER postgres PASSWORD 'newpassword';

# 2. CREATE THE DATABASE:
#    Run in PostgreSQL: CREATE DATABASE notorix;

# 3. TEST CONNECTION:
#    Run: node test-db-connection.js (we'll create this file)

# 4. UPDATE THE PASSWORD:
#    Replace 'your_password_here' above with your actual password

# =================================
# EXAMPLE WORKING CONFIGURATIONS:
# =================================

# For local development with password 'admin':
# DATABASE_URL=postgresql://postgres:admin@localhost:5432/notorix

# For local development with no password:
# DATABASE_URL=postgresql://postgres@localhost:5432/notorix

# For Docker/Container setup:
# DATABASE_URL=postgresql://postgres:admin@db:5432/notorix 